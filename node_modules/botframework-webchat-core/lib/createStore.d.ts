import { Store } from 'redux';
export declare type State = {};
export declare type ChatStore = Store<State>;
export default function (initialState: any, ...middlewares: any[]): Store<State, import("redux").AnyAction>;
